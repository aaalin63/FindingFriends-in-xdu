# 本地开发默认不需要 DATABASE_URL，会使用 xdu_partner.db。
# 部署到云端时填写 PostgreSQL 连接串，例如 Render/Railway/Supabase 提供的连接地址。
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME
PORT=8000
HOST=0.0.0.0
SQLITE_DB_PATH=xdu_partner.db
